module Interface
  module IRely
    class CredentialParser
      def inititalize(credentials)
        @credentials = credentials
      end

      # TODO: Parse the details into key, secret, and company id
    end
  end
end
